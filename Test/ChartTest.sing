if(!defined(Scheme))
{
    LIB "scheme.lib";
}
killall();

ring R=0,(x,y,z),dp;
chart U=ideal(x,y);
U;
U=StdOf(U);
dim(U);
isEmpty(U);
chart V=ideal(1);
V;
U!=V;
U==U;

def S=RingOf(U);
setring S;
ValueOf(U);